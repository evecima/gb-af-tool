# Apartment Finder v3 map asset

This branch is a release candidate. It must not be merged into `main` until the exact, approved image is added at `map/site-plan.png`.

The image is an illustrated public-facing property map; do not include boiler rooms or other sensitive facility details.
